#version 460

layout (location = 0) in vec2 VertexPosition;

uniform vec2 resolution;
uniform vec4 color;

void main() {
	vec2 tempPosition = ((VertexPosition / resolution) * 2.0f) - 1.0f;
	gl_Position = vec4(tempPosition * vec2(1.0, -1.0), 0.0f, 1.0f);
}